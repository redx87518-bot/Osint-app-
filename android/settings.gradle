pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven { url = uri("https://github.com/jan-tennert/supabase/raw/master/releases") }
    }
}

rootProject.name = "JarvisAI"
include(":app")
